<UserControl x:Class="pricecheckingtool.Views.StashTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:pricecheckingtool.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ViewModels="clr-namespace:pricecheckingtool.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200"
             d:DataContext="{d:DesignInstance ViewModels:UserViewModel}">

    <ListView ItemsSource="{Binding StashTabs}" Background="#FFFDFDFD">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="StashTabName" Width="100" DisplayMemberBinding="{Binding name}"/>
                <GridViewColumn Header="StashTabType" Width="100" DisplayMemberBinding="{Binding type}"/>
            </GridView>
        </ListView.View>
    </ListView>
    
</UserControl>
