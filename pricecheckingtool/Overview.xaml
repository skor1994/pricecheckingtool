<Window x:Class="pricecheckingtool.Overview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pricecheckingtool"
        mc:Ignorable="d"
        Title="Overview" Height="600" Width="800" ResizeMode="CanMinimize">
    <Grid>
        <ListView MouseLeftButtonUp="ListViewTabs_MouseLeftButtonUp" x:Name="listViewTabs" Background="#FFFDFDFD" Height="auto" Width="auto" HorizontalAlignment="Left" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="StashTabName" Width="auto" DisplayMemberBinding="{Binding name}"/>
                    <GridViewColumn Header="StashTabType" Width="auto" DisplayMemberBinding="{Binding type}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView x:Name="listViewItems" Height="auto" Width="auto" HorizontalAlignment="Right" Margin="50" Background="#FFFDFDFD">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ItemName" Width="auto" DisplayMemberBinding="{Binding name}"/>
                    <GridViewColumn Header="ItemTypeLine" Width="auto" DisplayMemberBinding="{Binding typeLine}"/>
                    <GridViewColumn Header="ItemRarity" Width="auto" DisplayMemberBinding="{Binding itemRarity}"/>
                    <GridViewColumn Header="StackSize" Width="auto" DisplayMemberBinding="{Binding stackSize}"/>
                    <GridViewColumn Header="ItemValue" Width="auto" DisplayMemberBinding="{Binding value}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Click="Button_Click" VerticalAlignment="Bottom" Height="Auto" Width="125" HorizontalAlignment="Right" Margin="0,0,100,25">Refresh</Button>
    </Grid>
</Window>
